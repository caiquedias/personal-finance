# #369 — Expurgo - Análise Detalhe
Issue: #369 | Data: 2026-06-29 | Branch: feat/369-expurgo-analise-detalhe

## Arquivos criados
_(nenhum)_

## Arquivos modificados
- `personal-finance/src/app/core/services/csv-reader.service.ts` — parser reescrito para 12 colunas (REQUIRED_COLUMNS=12); parseCsvLine RFC 4180 com suporte a campos entre aspas; mapas enum string→número (FORTNIGHT_TYPE_MAP, PAYMENT_STATUS_MAP, SOURCE_TYPE_MAP) com fallback ??; receivedAt de Income sintetizado como "MM/YYYY"; IDs sintéticos para @for tracking; FortnightType.First como default para Income
- `personal-finance/src/app/core/services/csv-reader.service.spec.ts` — spec reescrita para novo formato de 12 colunas; 30 testes: parsing Expense/Income, aspas RFC 4180, enums como string, receivedAt sintetizado, fallback para string inválida
- `personal-finance/src/app/features/purge/purge-detail.component.ts` — coluna "Quinzena" removida da tabela Income (FortnightType vazio no CSV para Income); header "Recebido em" → "Período"

## Decisões técnicas
- `receivedAt` sintetizado como "MM/YYYY" (ex: "01/2024") em vez de campo real — Income não tem receivedAt no CSV, apenas PeriodYear/PeriodMonth
- `FortnightType.First` como default para Income (campo não exportado no CSV); coluna Quinzena removida do template para evitar exibição incorreta
- IDs sintéticos (`expense-N`, `income-N`) para manter `@for track exp.id` sem alterar interface `ExpenseResponse`/`IncomeResponse`
- Constante `IDX_CATEGORY = 5` removida — categoryId fixado como `''` pois Category (nome) não está disponível sem join no backend

## Desvios do spec
- Nenhum. Root cause e fixes alinhados com o planning do comentário da issue.

## Estado do sistema após esta issue

### Front-end
- **Serviços:** `CsvReaderService` corrigido — aceita CSV de 12 colunas exportado pelo backend; parser RFC 4180; enums como string mapeados para valores numéricos
- **Componentes:** `PurgeDetailComponent` atualizado — tabela Income com 4 colunas (Descrição, Valor, Período, Notas)
