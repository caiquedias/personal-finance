# #332 — [FE] Expurgo: MOD-03 — Registro de Expurgos
Issue: #332 | Data: 2026-06-26 | Branch: feat/332-expurgo-mod-03-registro-de-expurgos

## Arquivos criados
- nenhum

## Arquivos modificados
- `personal-finance/src/app/core/models/models.ts` — interface `PurgeRecordResponse` adicionada (id, year, month, purgedAt, totalIncome, totalExpense, itemCount)
- `personal-finance/src/app/core/services/api.service.ts` — métodos `getPurgeRecords()` (GET /purge/records) e `deletePurgeRecord(id)` (DELETE /purge/records/{id}) adicionados
- `personal-finance/src/app/features/purge/components/purge/purge.component.ts` — signals `purgeRecords`, `deleteRecordModalOpen`, `selectedRecord`; métodos `openDeleteRecordModal`, `closeDeleteRecordModal`, `confirmDeleteRecord`; `PurgeWarningBannerComponent` importado; `confirmPurge()` agora recarrega `purgeRecords` após expurgo
- `personal-finance/src/app/features/purge/components/purge/purge.component.spec.ts` — 22 novos testes (Red + correções de Reviewer)

## Decisões técnicas
- `confirmPurge()` faz reload via `getPurgeRecords()` após expurgo — backend cria novo PurgeRecord, reload garante consistência
- `confirmDeleteRecord()` usa filtro local (sem reload) — delete de metadados não gera novo registro, filtro suficiente
- Teste de metadados `ɵcmp.dependencies` substituído por verificação DOM — incompatibilidade com Angular 21 JIT mode
- Import local de `PurgeRecordResponse` no spec removido após Reviewer apontar dead code

## Desvios do spec
- `purgedAt` exibido como ISO string raw (tech debt registrado) — formatação com DatePipe deixada para issue futura
- Issue #356 criada para aplicar layout visual (tabela padrão + modal Sonic) — não estava no escopo desta issue

## Estado do sistema após esta issue

### Front-end
- **Componentes:** PurgeComponent (`features/purge/components/purge/`) — agora inclui listagem de PurgeRecords e modal de delete de metadados
- **Serviços:** ApiService com métodos `getPurgeRecords()` e `deletePurgeRecord()` adicionados
- **Modelos:** `PurgeRecordResponse` disponível em models.ts
