# #378 — Expurgo - Análise Detalhe
Issue: #378 | Data: 2026-06-29 | Branch: feat/378-expurgo-analise-detalhe

## Arquivos criados
- `personal-finance/src/app/features/purge/purge-detail.component.spec.ts` — 76 testes Jasmine para PurgeDetailComponent (criado do zero)

## Arquivos modificados
- `personal-finance/src/app/features/purge/purge-detail.component.ts` — refatoração completa: header, abas, filtros compartilhados, grid padronizado, KPIs

## Decisões técnicas
- **Filtros compartilhados** (não por aba): a issue pede filtros aplicados nas 3 abas — `filterDesc` e `filterFortnight` são sinais únicos que afetam `filteredExpenses`, `filteredIncomes` e os KPIs simultaneamente. Filtros por aba foram planejados na issue #384.
- **CurrencyBrlPipe** substituiu `number:'1.2-2'` no grid para consistência com as demais telas.
- **Classes `.table`/`.table-wrap`** copiadas localmente nos `styles[]` do componente (não globais) — pattern de period-detail.
- **Índice Ivy `[0][0]`**: testes de import via `ɵcmp.selectors` usam índice `[0][0]` (element selectors), não `[0][1]` (attribute selectors) — corrigido no spec após fase Red.

## Desvios do spec
- `filterFortnight` não filtrou receitas na prática (CSV exporta incomes sempre com `FortnightType.First` hardcoded) — campo visível no modal mas sem efeito real em receitas; tratado como tech debt na issue #384.

## Estado do sistema após esta issue

### Front-end
- **Rotas:** sem alteração (`purge/analysis/detail` já existia)
- **Componentes:**
  - `PurgeDetailComponent` — refatorado com: `HeaderComponent`; 3 abas (`activeTab: Signal<'expenses'|'incomes'|'indicators'>`); `FilterModalComponent` + `FilterButtonComponent`; computed signals `filteredExpenses`, `filteredIncomes`; KPIs `kpiTotalIncome`, `kpiTotalExpense`, `kpiTotalPaid`, `kpiTotalOwed`, `kpiBalance`, `kpiPaymentProgress`; grid com `.table`/`.table-wrap`, badges de status, `CurrencyBrlPipe`, ícones de sort; `statusBadgeClass()`, `expSortIcon()`, `incSortIcon()`
- **Serviços:** sem alteração
